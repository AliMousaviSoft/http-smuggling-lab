services:
  backend:
    build: ./docker/backend
    container_name: smuggling-backend
    ports:
      - "3000:3000"

  nginx:
    build: ./docker/nginx
    container_name: smuggling-nginx
    ports:
      - "8000:8000"
    depends_on:
      - backend
